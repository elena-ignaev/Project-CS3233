TODO:
in uml diagram
make changes to tubepane class: add constructor tubepane(testtube), remove constructor tubepane(heights, testtube)
error of flicking dragging animation and transparent tab pane can see through the components behind
add blue litmus class
using regex for formatting of database document
use arraylist instead of array in database --> related problems in cation and anion --> need to change in part 1

use media for tutorial on how to use the application
use separate thread to update GUI

think about resizing object

use property binding to allow for rescaling of object when resizing the application window

if the application is to be extended, could implement allowing teacher to add their own salts of interest, providing them with the legal color list in javafx

add animation to buttons in substance

When adding a known salt, remember to open up a tab "Explanation"

When adding a random salt, remember to open up a tab "Question" to test for knowledge